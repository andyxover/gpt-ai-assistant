# Public — safe to ship to the browser
NEXT_PUBLIC_SUPABASE_URL=https://qhzectszimllhcciyrbq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...

# Server-only — service role bypasses RLS. NEVER expose to browser.
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Direct Postgres URL (same one the backbone uses) — for server-side queries
# that reuse the tcs-tutor/app services.
DATABASE_URL=postgresql://postgres.qhzectszimllhcciyrbq:[YOUR-PASSWORD]@aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres

# Optional — Anthropic key for any AI features the web app calls directly
# (most AI work runs in the tcs-tutor/app backbone).
ANTHROPIC_API_KEY=sk-ant-...
